# EVENT 접수페이지 아이콘

이 폴더에 SVG를 넣으면 `page_form.html`이 자동으로 표시합니다.
(파일이 없으면 임시로 ✕ 가 보입니다. 올리고 push하면 즉시 반영 — GitHub Pages)

## 파일명 (정확히)

| 용도 | 파일명 |
|------|--------|
| 오른손 | `hand-right.svg` |
| 왼손 | `hand-left.svg` |
| 종류 — 블런트 | `cat-blunt.svg` |
| 종류 — 틴닝 | `cat-thinning.svg` |
| 종류 — 장가위 | `cat-long.svg` |
| 종류 — DRY | `cat-dry.svg` |

> 가격(5/10/20만)은 글씨만 쓰기로 해서 아이콘 미사용.

## 상단 배너 (PNG)
| 용도 | 파일명 |
|------|--------|
| 상단 안내 배너 | `event-top.png` |
- 권장: **가로 1080px 고정**, 세로는 내용에 맞게(예 600~900px). 표시는 가로 100%로 자동 스케일. 없으면 자동 숨김.

## 규칙
- 권장: 정사각 SVG (손/종류 ~64×64). 표시는 CSS가 30/22px로 맞춤.
- 경로: `projects/event/icons/<파일명>` → 서빙 URL `page.mamoru.kr/projects/event/icons/<파일명>`
