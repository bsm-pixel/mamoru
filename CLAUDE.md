# 🤖 CLAUDE.md - MAMORU Project Rules

이 프로젝트의 모든 작업 시 아래 원칙을 **최우선(Priority 1)**으로 준수하시오.

## 1. 🛡️ 기능 보존의 절대 원칙 (Safety First)
- **로직(Logic) 100% 유지:** 디자인(UI/UX)이나 CSS를 수정할 때, 기존의 `<script>` 로직, `id`, `class`명을 함부로 변경하여 기능이 작동하지 않는 일이 없도록 주의할 것.
- **명시적 요청 시에만 수정:** 기능(JavaScript) 수정 요청이 명시적으로 없는 경우, UI만 변경하고 로직은 기존 상태를 완벽히 유지해야 한다.
- **수정 전 확인:** 코드를 수정하기 전에 반드시 `read_file`을 수행하여 현재 파일 구조와 로직을 파악할 것. (추측 금지)

## 2. 💰 토큰 절약 및 출력 최적화 (Token Economy)
- **잡담 금지:** "알겠습니다", "작업을 시작합니다" 같은 불필요한 대화체는 생략하고, 바로 결과물이나 질문만 출력할 것.
- **전체 코드 출력 지양:** 수정된 파일의 전체 코드를 다시 출력하지 마시오.
  - **Diff 방식:** 변경된 부분만 보여주되, 사용자가 위치를 찾을 수 있도록 앞뒤 문맥(Context)을 포함하여 `` 형태로 생략할 것.
- **설명 간소화:** 코드 변경 이유는 장황하게 설명하지 말고, 코드 옆에 주석(Comment)으로 짧게 남길 것.

## 3. 📝 코드 스타일 및 작업 흐름 (Workflow)
- **병합(Merge) 용이성:** 제안하는 코드는 복사해서 바로 붙여넣었을 때 기존 코드와 충돌 없이 작동하도록 작성할 것.
- **주석 필수:** 수정된 라인에는 주석을 달아 어느 부분이 왜 바뀌었는지 표시할 것.
- **작은 단위 커밋:** 기능 하나가 완성되면 즉시 `git commit`을 제안할 것. (대규모 수정 후 에러 방지)
- **언어:** 모든 설명과 커밋 메시지는 **'한국어'**로 작성할 것.

## 4. 🌐 아임웹 코드위젯 연동 (Imweb Integration)
- **GitHub Pages 호스팅 파일:** `DiagRef.html`, `RecommendPage.html` 등은 git push만 하면 자동 반영됨 (코드위젯 수정 불필요)
- **코드위젯 수정 필요 시:** iframe URL이 바뀌거나, 코드위젯 자체 코드(`ImwebIframeCode_*.html`)가 변경될 때만 안내할 것:
  > "아임웹 디자인모드 코드위젯에 로컬 파일 내용을 붙여넣으세요"

## 5. 🧠 맥락 유지 (Context Retention)
- **기억 의존 금지:** 이전 대화 내용을 기억하려 하지 말고, 현재 파일의 상태를 기준으로 판단할 것.
- **단일 파일 집중:** 사용자가 특정 파일을 지칭하면, 연관 없는 다른 파일까지 불필요하게 건드리지 말 것.

## 6. 🎨 디자인 가이드 (MAMORU Style)
- **Tone:** 신뢰감 있는, 미니멀한, 블랙/화이트(Black & White) 톤 유지.
- **UX:** 사용자 클릭 동선을 최소화하고 직관적인 UI를 설계할 것.

## 7. 📱 반응형 디자인 필수 (Responsive Design)
- **PC/모바일 분리 설계:** 동일한 레이아웃을 공유하지 말고, 각 환경에 최적화된 배치를 적용할 것.
- **모바일 우선 체크리스트:**
  - 모든 선택지/콘텐츠가 스크롤 없이 또는 자연스러운 스크롤로 보이는지 확인
  - 브라우저 하단 UI(주소창, 툴바)에 요소가 가려지지 않도록 `fixed` 사용 자제
  - 그리드 배치 시 모바일에서 잘리지 않도록 컴팩트하게 조정
- **PC 레이아웃:**
  - 콘텐츠 영역 내에서 버튼/요소 배치 (화면 끝에 붙이지 않음)
  - 가독성과 시각적 균형 고려

## 8. 🔒 수정 완료 상태 유지 (Fix Preservation)
- **수정 완료 코드 보존:** 이전 대화에서 문제 제기 후 수정 완료된 코드는,
  사용자가 별도로 재수정을 요청하지 않는 한 해당 상태를 100% 유지해야 한다.
- **임의 롤백 금지:** 수정된 로직, 버그 픽스, 기능 개선을 임의로
  이전 버전으로 되돌리지 말 것.
- **예시:** iframe 높이 무한루프 버그 수정 → 수정된 debounce 로직 유지